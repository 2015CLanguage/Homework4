#include<stdio.h>
 int main()
 {int a,b,c,m,n;
 printf("请输入两个正整数m,n:");
 scanf("%d,%d",&m,&n);
 if(m>n) {c=m;
         m=n;
		 n=c;}
 a=m*n;
 while(m!=0)
 {b=n%m;
 n=m;
 n=b;}
printf("最大公约数为:",n);
printf("最小公倍数为:",a/n);
return 0;}
