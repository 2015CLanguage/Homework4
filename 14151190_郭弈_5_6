#include<stdio.h>
#include<math.h>
int main()
{
    int a,i,j,sum;
    a=1,i=1,j=1,sum=0;
	  while(a<=20)
	  {
		    while(i<=a)
		    {
		        j=i*j;
		        i=i+1;
		    }
        sum=sum+j;
		    a=a+1;
	  }
	  printf("%d\n",sum);
	  return 0;
}
